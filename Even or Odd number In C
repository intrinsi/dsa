#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    if(num%2==0)
    {
        printf("%d is the Even Number",num);
    }
    else
    {
        printf("%d is the Odd Number",num);
    }
}
